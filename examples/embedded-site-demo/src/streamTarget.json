{
  "$comment": "Build-time placeholders. The container entrypoint substitutes __OWV_SERVER__ / __OWV_PORT__ from SIGNALING_SERVER / SIGNALING_PORT (env or /etc/host.yaml) on every boot -- same mechanism as the main viewer. For `npm run dev` (no entrypoint) override at runtime: open ?server=<ip>&port=<port>.",
  "server": "__OWV_SERVER__",
  "signalingPort": "__OWV_PORT__"
}
